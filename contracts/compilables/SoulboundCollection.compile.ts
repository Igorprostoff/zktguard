// Blueprint ≥0.27 with `separateCompilables: true` resolves compile
// configs from compilables/; the canonical config stays in wrappers/.
export { compile } from "../wrappers/SoulboundCollection.compile";
